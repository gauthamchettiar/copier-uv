"""{{ project_description }}"""


def hello(name: str = "World") -> str:
    """Return a greeting message.

    Args:
        name: The name to greet.

    Returns:
        str: A greeting message from the {{ project_slug }} package.
    """
    return f"Hello, {name} from {{ project_slug }}!"
