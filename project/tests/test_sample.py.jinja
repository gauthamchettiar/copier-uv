"""Sample tests for {{ project_slug }}."""

from {{ project_slug }} import hello


def test_hello() -> None:
    """Test the hello function."""
    result = hello()
    assert isinstance(result, str)
    assert "Hello from {{ project_slug }}" in result


def test_hello_returns_non_empty_string() -> None:
    """Test that hello returns a non-empty string."""
    result = hello()
    assert result
    assert len(result) > 0
